import{createClient}from"@/lib/supabase/server";
const stages=["prospecting","contacted","demo","proposal","won"];
const labels:{[k:string]:string}={prospecting:"Prospectando",contacted:"Contatado",demo:"Demo",proposal:"Proposta",won:"Fechado"};
export default async function Home(){const s=await createClient();const[{data:prospects},{data:proposals},{data:clients}]=await Promise.all([s.from("prospects").select("*").order("created_at",{ascending:false}).limit(60),s.from("proposals").select("*"),s.from("clients").select("*")]);const ps=prospects||[];const pp=proposals||[];const pipeline=pp.filter(x=>["draft","sent","negotiating"].includes(x.status)).reduce((n,x)=>n+Number(x.amount||0),0);return <div className="shell"><aside className="side"><div className="brand">ISA <span>TECINFO</span></div><div className="muted">BUSINESS OS</div><nav className="nav"><a href="/">Visão geral</a><a href="/prospects">Prospecção</a><a href="/api/agent/run">Agentes</a><a>Clientes</a><a>Propostas</a><a>Financeiro</a><a>Projetos</a><a>Configurações</a></nav></aside><main><div className="top"><div><div className="eyebrow">Operação comercial semiautônoma</div><h1>Command Center</h1></div><div className="badge">Modo supervisionado</div></div><div className="grid"><div className="card"><div className="muted">Prospects</div><div className="metric">{ps.length}</div></div><div className="card"><div className="muted">Propostas abertas</div><div className="metric">{pp.filter(x=>x.status!=="accepted").length}</div></div><div className="card"><div className="muted">Clientes</div><div className="metric">{(clients||[]).length}</div></div><div className="card"><div className="muted">Pipeline</div><div className="metric">{pipeline.toLocaleString("pt-BR")}</div></div></div><section className="section"><h2>Pipeline</h2><div className="pipeline">{stages.map(st=><div className="card col" key={st}><b>{labels[st]}</b>{ps.filter(p=>p.stage===st).map(p=><div className="lead" key={p.id}><b>{p.business_name}</b><div className="muted">{p.niche||"Sem nicho"} · {p.country||""}</div><div className="badge">{p.grade||"—"}</div></div>)}</div>)}</div></section><section className="section"><h2>Fluxo dos agentes</h2><div className="flow">PROSPECÇÃO → AUDITORIA → SOLUÇÃO → DEMO → CONTATO → NEGOCIAÇÃO → PROPOSTA → CONTRATO → PAGAMENTO → APROVAÇÃO → PUBLICAÇÃO</div></section></main></div>}