import {canAutoSend}from "@/lib/commercial-rules";import type{Offer,ProspectInput}from "./types";
export function firstContact(p:ProspectInput,o:Offer){const en=(p.country||"").toLowerCase()!=="brazil"&&(p.country||"").toLowerCase()!=="brasil";return en?`Hi! I'm contacting you on behalf of Isa TecInfo. We reviewed the public digital presence of ${p.business_name} and found a few opportunities to improve how customers find and contact the business. We prepared a short concept and can share it with no commitment. If useful, I can also explain scope, price and timeline.`:`Olá! Falo em nome da Isa TecInfo. Analisamos a presença digital pública da ${p.business_name} e identificamos algumas oportunidades para melhorar a forma como clientes encontram e entram em contato com a empresa. Preparamos um conceito curto, sem compromisso. Se fizer sentido, posso apresentar também escopo, valor e prazo.`}
export function infrastructureQuestion(country?:string|null){return(country||"").toLowerCase()==="brazil"?"Vocês já possuem domínio e hospedagem/servidor? Se não tiverem, a Isa TecInfo pode providenciar a infraestrutura após o fechamento, conforme a proposta.":"Do you already have a domain and hosting/server? If not, Isa TecInfo can arrange the infrastructure after closing, as described in the proposal."}
export function salesAction(kind:string){return{allowed:canAutoSend(kind),requires_human:!canAutoSend(kind)}}